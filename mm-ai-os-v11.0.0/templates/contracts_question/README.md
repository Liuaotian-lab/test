# Question Contract Templates

`question-activate` 或 `contract-build` 会把本目录中的模板复制到：

```text
cases/<case_id>/contracts/questions/<QID>/
```

v5.1 的小问 contract 包含：问题定义、变量、目标、约束、验证、预算、缺失信息、质量声明、工具包、运行时、输出、证据和报告要求。
