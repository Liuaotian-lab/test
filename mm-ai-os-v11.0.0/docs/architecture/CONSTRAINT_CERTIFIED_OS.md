# Constraint-certified MM-AI OS

MM-AI OS 10.x upgrades the workflow from artifact completeness to mathematical trust. The required trust chain is:

problem evidence → constraint ledger → dependency graph → solver hook → validator → negative test → certificate → independent verification → evidence claim → final claim → final gate v2.
