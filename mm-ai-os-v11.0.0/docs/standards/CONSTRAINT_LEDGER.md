# Constraint Ledger Standard

Every active constraint must map to solver hooks, validators, negative tests and, for optimality/boundary claims, certificates. Coverage below 100% blocks strict final delivery.
