# Model Tournament Requirements

Every required question must have:

- L0 naive baseline
- L1 contest baseline
- L2 advanced competition model
- L3 innovation/hybrid/domain-specific model

Model cards must include variables, objective, constraints, data inputs, verification plan, complexity, expected weakness, and paper value.
