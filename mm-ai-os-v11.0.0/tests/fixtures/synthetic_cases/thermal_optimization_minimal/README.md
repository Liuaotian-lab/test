# Synthetic Thermal Optimization Minimal Case

This fixture is the v7.5 deterministic end-to-end case. It is intentionally small and does not require web search, LLM calls, OCR, or LaTeX tools. External knowledge is represented by deterministic fixture search results injected through `synthetic-search-inject`.
